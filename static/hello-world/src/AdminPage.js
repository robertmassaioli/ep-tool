import React from 'react';
import { AdminSettings } from './AdminSettings';

export function AdminPage() {
  return (
    <div>
      {/* Admin page wrapper - could add admin-specific navigation or context here */}
      <AdminSettings />
    </div>
  );
}
